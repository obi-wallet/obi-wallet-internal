import { vertexTheme } from "@obi-wallet/theme";

import { obiModalConfig } from "./obi";
import { Config } from "../config";

export const vertexModalConfig: Config = {
  ...obiModalConfig,
  theme: vertexTheme,
};
