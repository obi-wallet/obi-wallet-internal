import { InitialBackground } from "@obi-wallet/common";
import { observer } from "mobx-react-lite";

export const SplashScreen = observer(function SplashScreen() {
  return <InitialBackground />;
});
